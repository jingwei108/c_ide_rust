key: matrixChain
name: 矩阵连乘
category: 动态规划

params:

tutorial:
  steps:
    - title: 状态定义
      description: dp[i][j] 表示计算矩阵 Ai..j 的最少乘法次数。i == j 时为 0（单个矩阵无需乘法）。
      anchor: 状态定义

knowledge_nodes:
  - Algorithm