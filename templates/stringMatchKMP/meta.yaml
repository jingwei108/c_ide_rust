key: stringMatchKMP
name: KMP 模式匹配
category: 字符串

params:

tutorial:
  steps:
    - title: next 数组
      description: KMP 的核心是 next 数组，next[j] 表示当 T[j] 失配时，j 应该回溯到的位置。利用模式串自身的重复结构避免主串指针回溯。
      anchor: next_数组

knowledge_nodes:
  - Algorithm