key: criticalPath
name: 关键路径
category: 图算法

params:

tutorial:
  steps:
    - title: 拓扑排序与最早时间
      description: 先进行拓扑排序，同时计算每个事件的最早发生时间 ve。ve[j] = max(ve[i] + weight(i,j))。
      anchor: 拓扑排序与最早时间

knowledge_nodes:
  - Algorithm