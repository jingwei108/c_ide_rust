key: staticLinkedList
name: 静态链表
category: 结构

params:

tutorial:
  steps:
    - title: 静态链表结构
      description: 静态链表用数组模拟链表，每个数组元素包含 data 和 cur（游标）。游标存放下一个元素的下标，0 表示空。
      anchor: 静态链表结构

knowledge_nodes:
  - Algorithm