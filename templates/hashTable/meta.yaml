key: hashTable
name: 哈希表
category: 结构

params:

tutorial:
  steps:
    - title: 哈希与冲突
      description: 哈希表通过哈希函数把关键字映射到数组下标。当两个关键字映射到同一位置（冲突）时，用线性探测法向后找空位。
      anchor: 哈希与冲突

knowledge_nodes:
  - Algorithm