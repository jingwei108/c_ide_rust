key: insertion
name: 插入排序
category: 排序

params:
  n:
    label: 数组长度
    type: int
    default: 5

tutorial:
  steps:
    - title: 外层循环
      description: 插入排序把数组分成"已排序"和"未排序"两部分。i 从 1 开始，因为第 0 个元素默认已排序。
      anchor: 外层循环

knowledge_nodes:
  - Algorithm