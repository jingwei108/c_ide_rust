key: topologicalSort
name: 拓扑排序
category: 图算法

params:

tutorial:
  steps:
    - title: 计算入度
      description: 拓扑排序先计算每个顶点的入度（指向该顶点的边数），然后把所有入度为 0 的顶点入队。
      anchor: 计算入度

knowledge_nodes:
  - Algorithm