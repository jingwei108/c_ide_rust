key: gcd
name: 辗转相除
category: 基础

params:
  a:
    label: 第一个数
    type: int
    default: 48
  b:
    label: 第二个数
    type: int
    default: 18

tutorial:
  steps:
    - title: 欧几里得算法
      description: 辗转相除法的核心：gcd(a,b) = gcd(b, a mod b)。当 b 变为 0 时，a 就是最大公约数。
      anchor: 欧几里得算法

knowledge_nodes:
  - Algorithm