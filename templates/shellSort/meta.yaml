key: shellSort
name: 希尔排序
category: 排序

params:
  n:
    label: 数组长度
    type: int
    default: 5

tutorial:
  steps:
    - title: 增量序列
      description: 希尔排序先取一个增量 gap，把数组分成 gap 组，每组内部进行插入排序。然后逐步缩小 gap，最后 gap=1 时就是普通的插入排序。
      anchor: 增量序列

knowledge_nodes:
  - Algorithm