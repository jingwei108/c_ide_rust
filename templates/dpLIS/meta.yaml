key: dpLIS
name: 最长递增子序列
category: 动态规划

params:

tutorial:
  steps:
    - title: 状态定义
      description: dp[i] 表示以 arr[i] 结尾的最长递增子序列长度。初始时每个元素自身构成长度为 1 的 LIS。
      anchor: 状态定义

knowledge_nodes:
  - Algorithm