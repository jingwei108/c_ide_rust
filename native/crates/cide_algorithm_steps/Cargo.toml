[package]
name = "cide_algorithm_steps"
version = "0.1.0"
edition = "2021"

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(frb_expand)'] }

[lints.clippy]
unwrap_used = "deny"
expect_used = "warn"
missing_panics_doc = "allow"
too_many_lines = "warn"
type_complexity = "allow"
