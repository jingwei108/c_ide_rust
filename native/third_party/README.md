# Third Party Dependencies

This project currently uses a custom CideVM runtime and does not depend on any external third-party libraries for core compilation or execution.

If future dependencies are added, they should be documented here with setup instructions.
