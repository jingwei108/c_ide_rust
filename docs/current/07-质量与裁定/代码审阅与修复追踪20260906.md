# Cide 项目全面代码审阅报告（2026-09-06）

> 审阅范围：全仓库 — Rust 编译器管线（10 个子 crate）、CideVM 运行时、统一模式/时间旅行引擎、FRB/C API 桥接层、诊断系统、Flutter 前端（107 个 Dart 文件）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕、工程体系（CI/脚本/测试防线/文档）
> 审阅方法：5 路并行深度代码审阅（逐文件精读 + Grep 模式定位）+ 基线验证（cargo clippy / dart analyze / cargo test --workspace --lib / cargo tree）。**所有标注「实测」的问题均用 release 版 `cide_cli` 或独立 Rust 程序复现确认，非推测。**
> 说明：本报告承接 2026-06-13 的《code_review_report.md》，覆盖其后新增的 Phase 30~42 代码。约 137 条有 `file:line` 证据的发现：P0 × 25、P1 × 56、P2 × 56。
> **2026-09-11 翻新口径（前端切割后）**：本报告仍是**修复进度的权威追踪**（主计划 §8 指定），**不归档**。前端切割（`CideFlutter/`、FRB 桥接、全部 Flutter 构建脚本与 web 部署 workflow 迁出，标签 `before-frontend-split`）后，**所有 Flutter/Dart 侧条目（U-P0-*、U-P1-*、前端 P2 条目、`dart analyze` 基线）一律冻结：条目原文与编号全部保留、追踪表不删行，仅追加「前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进」标注**。**Rust 侧未关闭条目（V-P0-3 后端侧、typeck/codegen 剩余项、临时槽位分配器重构、V-P1-* 等）不受影响，继续按第 6 节路线追踪。**

---

## 0. 修复进度追踪（2026-09-06 起）

**✅ 第一批（崩溃止血）已全部完成**（2026-09-06，详见 CHANGELOG [Unreleased]「崩溃止血」节）：

| # | 项 | 修复方式 | 验证 |
|---|---|---|---|
| 1 | V-P0-3 统一模式三入口 catch_unwind | 照抄 `execute_run` B47 模式，panic 后 VM 归还 session | 代码审查 + clippy 全绿 |
| 2 | V-P0-1/2/4 Mod/DivQ/ModQ/NegQ + JIT tpl_div/tpl_mod/tpl_neg | 统一补 `MIN / -1`、`MIN` 取反防护，转教学 trap | release cide_cli 实测 4 用例全部 trap |
| 3 | F-P0-1 lexer 注释递归循环化 + parser 深度计数 | `next_token` loop 重派发；`MAX_PARSE_DEPTH=64`（实测每层括号 ~3KB 栈，300 层溢出 1MB 栈，故上限远低于原定 256）；`DeclaratorGuard.ptr_count` 上限 32 | release 实测：2 万注释/6 万花括号/5 万括号/10 万星号全部不再崩溃；40 层合法嵌套、60 层合法嵌套不受影响 |
| 4 | T-P0-8 自含 struct 环检测 | TypeChecker Pass 1 新增 `E3072_StructSelfContain` 循环包含检测（含互相包含/数组包裹）；`compute_type_size`（两处镜像）与 `type_contains_resource` 加 visiting 集合 | release 实测：自含/互相包含报 E3072；合法链表（指针成员）正常 |
| 5 | V-P0-5 qsort/bsearch/VFS checked_mul | `nmemb.checked_mul(size)` + 总量 ≤ VM 内存上限 | release 实测：`qsort(2^32, 2^32)` 静默拒绝，无 OOM |
| 6 | E-P1-6 apply_fix 中文 panic | 新增 `safe_byte_col`：字节边界优先、非边界回退字符索引语义 | 单元测试 3 断言（字节/字符/混合语义列） |

新增回归防线 `native/tests/crash_regression_tests.rs`（12 用例全绿）；全量测试防线（lib 79 + E2E 10/203/51 + JIT 91 + Fuzz 5 + C++ 31/28/2）无回归；clippy 0 警告、cargo fmt 干净。

实测补充发现：`MAX_PARSE_DEPTH` 初值 256 下 300 层括号仍栈溢出（防护触发前栈已耗尽），据此下调至 64 —— 每层括号嵌套的完整优先级解析链消耗 ~3KB 栈，远超预期。

**下一批待推进**：第二批（CI 门禁 4 条：E-P0-1~P0-4）→ 第三批（codegen soundness 8 条 + 顺带 T-P1-1/P1-2）→ 第四批（Flutter 编辑器与状态）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕。

**✅ 第二批（CI 门禁）已全部完成**（2026-09-06 同日）：

| # | 项 | 修复方式 | 验证 |
|---|---|---|---|
| 7 | E-P0-1 shadow_verify.py 退出码 | 与 C++ 版对齐：非预期差异 exit 1 | 全量 631 例实测 |
| 8 | E-P0-2 三层对账读 cargo 退出码 | `returncode==0` + `test result:` 行存在才 PASS | 单元验证 3 场景 + 全量跑通 exit 0 |
| 9 | E-P0-3 一致性 hard/soft 分级 | KNOWN 过期/文件缺失 → hard 计入退出码；失败提醒保持 soft（自由文本无法精确匹配，精确对账由 cide_e2e.rs KNOWN_* 常量承担） | 单元验证 3 路径 |
| 10 | E-P0-4 clang 预检 fail fast | `verify_clang_available()` exit 2 + 版本写入 JSON | 模拟缺失场景 exit 2 |

**门禁化立即暴露 4 例存量差异**（此前恒绿掩盖，均非新回归，已处置）：
- `kr_5_8`：用例自身缺陷（`atof` 未 `#include <stdlib.h>`，Clang 22 非法隐式声明被 `-Wno` 压制后 UB 输出错序）；Cide 输出与正确编译的 Clang 一致 → 修用例后转 match。
- `bTree_default` / `infixEvaluation_default` / `spfa_default`：E2E 防线 `KNOWN_TEMPLATE_FAILURES` 已记录的模板偏差 → shadow `KNOWN_FAILURE_CASES` 对齐为 known_issue（防线间双向监控）。

门禁化后基线：631 例 = 610 match + 5 known_issue + 16 cide_better，exit 0；`cide_e2e` 10/10 全过；`ci_three_tier_check.py` 全过 exit 0。

**✅ 第三批（codegen soundness）已全部完成**（2026-09-06 同日）：T-P0-1~8（含 F-P0-2/3）+ 顺带 T-P1-1，全部实测验证。固化 `baseline/codegen_soundness_regression.c`（Cide 与 Clang 输出逐行一致，baseline 314→317）。

修复过程的关键发现（对后续开发有价值）：
1. **T-P0-6 的地址槽/64 位中间值槽必须在 `enter_function` 重置**——`temp_slot0~3` 一直按函数重置（-1），新增槽若沿用编译器实例级缓存会跨函数复用 offset，在小帧函数越界踩踏（曾引入 9 个 baseline 链表/队列用例回归，开发中被 E2E 防线捕获后修复）。
2. **`infixEvaluation_default` 真因是编译器缺陷而非模板缺陷**（自增/自减作数组索引），此前被误记为"模板栈下溢"；第三批修复后 E2E 转绿，防线 5 双向监控（KNOWN 常量 + 测试断言）首次实战拦截。
3. float 在 VM 中两种表示并存：局部/全局槽按 f64 位（StoreLocalD），**内存元素按 4 字节 f32 位**（LoadMem/StoreMem + CastF2D/CastD2F 转换链）——自增路径已按此语义对齐。

**下一批待推进**：第四批（Flutter 编辑器与状态：U-P0-1~5、U-P1 优先项）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕+ **E-P1-1/2（后端侧，落点 `native/src/flutter_bridge.rs`，仍在追踪）** + 建议提前的 V-P1-6（栈缓冲区溢出检测）/ V-P1-12（无效 free 诊断）/ V-P1-13（scanf 字符流化）。

**✅ "提前插入"项（V-P1-6/12/13）+ 第四批 Flutter 首批已完成**（2026-09-06 同日）〔其中 Flutter 子项：前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕：

| # | 项 | 修复方式 | 验证 |
|---|---|---|---|
| V-P1-6 | 栈缓冲区溢出检测 | `FuncMeta.local_buffers` 编译期登记 → VM Call 帧携带 → `check_stack_buffer_capacity` 校验；strcpy/strcat 用户侧分发回 Host | 实测 strcpy/strcat/scanf %s 三场景 E3070 含变量名；合法路径不误伤 |
| V-P1-12 | 无效 free 诊断 | 三场景（活跃块内部/已释放块内部/无效地址）教学 trap；realloc(p,0) 同构 | 实测 free(p+1)/free(&x) 分场景诊断 |
| V-P1-13 | scanf 字符流化 | 虚拟字节流 + 映射表游标（与 getchar 共享），行间补逻辑 \n | 实测 "1 2\n3 4" 两次 %d 读出 1 2 |
| — | delete[] nullptr 判空 | V-P1-12 暴露的存量缺陷（cide_vec 空容器析构 ptr-4 wrap） | C++ E2E 2 个失败用例转绿 |
| U-P0-1 | WatchTab controller 泄漏 | 迁移 ConsumerStatefulWidget | dart analyze 全绿〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕 |
| U-P1-9/10 | Timer 生命周期 | dispose 补 _cancelLongPress / AutocompleteController 补 dispose+safeNotify | dart analyze 全绿〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕 |

实现要点：strcpy/strcat 的栈检测需 Host 路径——Bytecode Libc 版逐字节 StoreMem 无法做整体容量校验，用户侧分发切回 Host（libc 索引表与预编译产物不变）；`local_buffers` 经 codegen → session.compile → VM register_function 三级透传（最初遗漏后两级导致检测不触发，已修复）。

**第四批剩余待推进**：U-P0-2~5（编辑器渲染管线重构：TextPainter 缓存/gutter/shouldRepaint）、U-P1-1/3/4（IdeState 拆分/防重入/copyWith）〔均属前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕；**E-P1-1/2（run_auto_steps_stream 防重入 / Session 上限）属后端侧，仍在追踪队列**（落点 `native/src/flutter_bridge.rs`）。

---

## 1. 执行摘要

**总体评价**：项目的工程理念与文档文化在同类教学项目中属上乘——五层测试防线设计、`KNOWN_*_FAILURES` 双向监控、Golden-only-from-Clang、`#![forbid(unsafe_code)]`、clippy `unwrap_used=deny` 全绿、零重复依赖、CHANGELOG「诚实记录」文化都值得肯定。**但本次审阅发现纸面质量与实际行为之间存在系统性落差**：

1. **CI 防线存在"只报告不拦截"缺口**（工程 P0-1~P0-4）：C 版 Shadow Verification 脚本无退出码，任何回归恒绿；三层对账不进退出码；clang 缺失静默降级为"全量 cide_better"。
2. **编译器 codegen 存在 8 个实测复现的 soundness bug**（typeck/codegen P0-1~P0-8）：全局 `double g = 1` 得 0、`long long` 全局数组初始化错误、`double d++` 无效、struct char 成员赋值破坏相邻内存、同名 static 跨函数共享槽位等——全部漏过现有五层防线，因为 shadow 用例未覆盖这些边角组合。
3. **两类可崩掉整个 IDE 的输入**：深嵌套/长注释/多星号导致编译器**栈溢出**（前端 P0-1，4 处实测 SIGSEGV，无法被 catch_unwind 捕获）；自含 struct 导致 TypeChecker 无限递归崩溃（typeck P0-8）。
4. **VM 算术溢出 panic 击穿 FFI**（VM P0-1~P0-5）：`INT_MIN % -1` 在 release 下恒 panic；统一模式（时间旅行）API 无 catch_unwind，panic 直接穿越 FRB 边界导致 Flutter 进程 abort（FFI panic 为 UB）。〔**FRB 边界已于 2026-09-11 随前端切割移除**；本条后端侧 catch_unwind 已于第一批完成，见 §0〕
5. **Flutter 编辑器渲染管线有结构性缓存缺陷**（前端 P0-2~P0-5）：静态无界 TextPainter 缓存进程级泄漏、高亮层每帧重新 layout、gutter 不随滚动更新、shouldRepaint 比较失效；巨型 IdeState + 全工程 0 处 `select` 导致每敲一个字符全树重建。〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕

**修复优先级总路线**：P0 全部 25 条建议分四批落地（详见第 6 节）：①崩溃类（栈溢出防护、catch_unwind、算术溢出）→ ②CI 门禁（4 条）→ ③codegen 8 条 soundness → ④Flutter 编辑器缓存重构〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕。P1 中与教学检测目标直接冲突的漏报路径（栈缓冲区溢出零检测、无效 free 漏报、scanf 行式消费）建议提前。

---

## 2. 基线验证结果（本次实测）

| 检查项 | 结果 |
|---|---|
| `cargo clippy --workspace --all-targets` | ✅ 全绿，0 警告（与 AGENTS.md 声明一致） |
| ~~`dart analyze`（CideFlutter）~~〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕 | ✅ No issues found（2026-09-06 当时实测，前端代码已迁出） |
| `cargo test --workspace --lib` | ✅ 全部通过（约 79 个 lib 单元测试） |
| `cargo tree -d` 重复依赖 | ✅ 零重复；直接依赖 18 个 |
| 仓库体积 | 28MB，可接受；`native/src/vm/`（旧路径）与 MAUI 文件仅存于 git 历史 |
| 测试结构观察 | ⚠️ 编译器子 crate 的 lib 内单测多数为 0，测试几乎全部由 `native/tests/` 集成测试承载；`cide_codegen` 字节码生成本体零单测（`tests.rs` 仅 11 个工具函数测试） |

---

## 3. P0 发现（25 条 — 崩溃 / 错误结果 / 防线失效）

### 3.1 编译器前端（cide_lexer / cide_parser / cide_ast）— 3 条

| # | 问题 | 位置 | 证据摘要 |
|---|---|---|---|
| F-P0-1 | **无递归深度防护，常规粘贴输入即栈溢出**（实测 4 处 SIGSEGV，无法被 catch_unwind 捕获） | `cide_lexer/src/lib.rs:122-135`（注释/预处理分支 `return self.next_token()` 递归）；`cide_parser/src/stmt.rs:36-146`（`{{{{…}}}}` 6 万层崩）；`cide_parser/src/expr/primary.rs:216-243`（括号递归）；`cide_parser/src/type_.rs:273-284,407-548`（10 万个 `*` 崩，`DeclaratorGuard` 不限 ptr_count） | 实测：2 万行 `//c` 注释（80KB）→ `has overflowed its stack`；6 万层花括号崩；5 万层括号崩；10 万星号崩 |
| F-P0-2 | **`unsigned long long` 丢失 unsigned 修饰 + u32→i32 位截断** | `cide_parser/src/type_.rs:45-52`（`long long` 提前 return 跳过 unsigned/const 合成）；`cide_parser/src/expr/primary.rs:25-33`（`parse::<u32>() as i32` 截断） | 实测：`unsigned long long a = 4000000000ULL; printf("%lld", a)` 输出 **-294967296**，clang 输出 4000000000；`const long long` 的 const 同样丢失 |
| F-P0-3 | **enum 初始化器非纯字面量时静默采用错误值** | `cide_parser/src/decl.rs:679-683`、`:544-548`（`if let Expr::Literal` 匹配不到即保持旧值，不报错不求值） | 实测：`enum { NEG = -1, ZERO, BIG = 1+2 }` 输出 `0 1 2`，clang 输出 `-1 0 3`。`enum { X = -1 }` 是教学极常见写法 |

### 3.2 类型检查 / 代码生成（cide_typeck / cide_codegen）— 8 条（全部实测复现）

| # | 问题 | 位置 | 证据摘要 |
|---|---|---|---|
| T-P0-1 | **全局标量初始化不做隐式类型转换，`double g = 1` 得 0** | `cide_typeck/src/lib.rs:251-268`（Pass 2.5 只 check 不 cast）；`cide_codegen/src/lib.rs:370-372`（Literal 无条件 `globals_init_32`）；`stmt/var_decl.rs:124-126` 同 | 实测：`double gd = 1; float gf = 2;` → `gd=0.000000 gf=0.000000`（int 位模式写进 double 槽） |
| T-P0-2 | **全局 long long（数组元素/struct 字段）初始化写成 double 位模式** | `cide_codegen/src/init.rs:60-68`（`LongLiteral → (v as f64).to_bits()`）；`lib.rs:334` | 实测：`long long ga[2] = {1,2}`（全局）→ 输出 `0 0`；局部正确，证明仅 flatten 全局路径错 |
| T-P0-3 | **double/float 的 `++`/`--` 完全无效** | `cide_codegen/src/expr/unary.rs:145-201`（固定 `LoadLocal/PushConst/Add/StoreLocal` int 路径）；`unary.rs:11-32` `gen_mem_inc_dec` 全类型盲 | 实测：`double d = 1.5; d++;` → `d=1.500000`。typeck `ops.rs:197-203` 明确允许浮点自增，两端合谋 |
| T-P0-4 | **struct/class char 成员赋值写 4 字节，破坏相邻内存** | `cide_codegen/src/expr/assign.rs:564-630`（Member 分支 5 组 match 全缺 Char→StoreMemByte；对比 Index/Deref 分支都有）；`stmt/var_decl.rs:545-565` `emit_field_init` 同 | 实测：全局 `struct Q{int x;char c;} gq={111,'A'}; int gnext=777777;` 执行 `gq.c='B'` 后 `gnext=0`（越界覆盖） |
| T-P0-5 | **char 数组元素自增的进位污染相邻元素** | `cide_codegen/src/expr/unary.rs:16,26`（4 字节 LoadMem/StoreMem 读改写 1 字节元素） | 实测：`char cs[2]={255,5}; cs[0]++;` → `{0,6}`（进位污染 cs[1]） |
| T-P0-6 | **复合赋值内嵌套赋值写错目标（temp_slot0 冲突家族残留）** | `cide_codegen/src/expr/assign.rs:433-448`（Index 复合赋值地址存 slot0，内层 gen_assign line 471 再次取 slot0 覆盖）；Deref/Member/引用分支同构 | 实测：`a[0] += (b[0] = 5);` → `a0=1 b0=6`（应 a0=6, b0=5）。历史上 `side_effect_index.c` 修过同家族 bug，嵌套赋值场景仍在 |
| T-P0-7 | **不同函数的同名 static 局部变量共享同一槽位** | `cide_codegen/src/stmt/var_decl.rs:72-74`（`static_local_indices` 以裸变量名为 key，同名直接 return，第二函数初始化被丢弃） | 实测：`f(){static int x=1; x++;} g(){static int x=100; print x;}` → `g.x=2`（打印出 f 的值） |
| T-P0-8 | **自含 struct 导致编译器栈溢出崩溃（IDE 直接崩）** | `cide_typeck/src/lib.rs:99-113`（Pass 1 无自含检查）+ `cide_ast/src/lib.rs:32-77`（`compute_type_size` 无 visited 集合）；`cpp_class_layout.rs:61-78` 循环继承同 | 实测：`struct S { int a; struct S inner; };` → `has overflowed its stack`。学生写链表节点漏 `*` 即触发 |

### 3.3 VM 与运行时（cide_vm / cide_runtime / unified）— 5 条

| # | 问题 | 位置 | 证据摘要 |
|---|---|---|---|
| V-P0-1 | **`OpCode::Mod` 缺 `INT_MIN % -1` 防护，直接 panic** | `cide_vm/src/core/executor/arithmetic.rs:73-81`（Div 有防护 line 57-58，Mod 只查 b==0） | 实测：release 下 `attempt to calculate the remainder with overflow` panic（Rust `%` 溢出检查不受 debug/release 影响） |
| V-P0-2 | **`DivQ`/`ModQ` 缺 `i64::MIN / -1` 防护** | `cide_vm/src/core/executor/float.rs:201-217`（`NegQ` line 219-222 的 i64::MIN 取负同） | 实测：release panic `attempt to divide with overflow` |
| V-P0-3 | **统一模式（时间旅行）API 无 catch_unwind，panic 击穿 FFI** | `native/src/flutter_bridge.rs:602-629`（run_auto_steps；该文件为历史会话包装层，现由 `cide_cli` 消费，名称待收敛）、`:632-643`（seek_to_step）、`:646-681`（step_next_unified） | `execute_run` 有防护（`engine/session_ops.rs:114-115`），统一模式三入口直接裸调。V-P0-1/2 的 panic 穿越 FRB 边界 → Flutter 进程 abort（FFI panic 为 UB）〔**FRB 边界已于 2026-09-11 随前端切割移除；本条后端侧 catch_unwind 已于第一批完成，见 §0**〕 |
| V-P0-4 | **JIT 模板 `tpl_div`/`tpl_mod` 复刻同样 panic 路径** | `cide_vm/src/jit_templates.rs:264-284`（只查 b==0）；热点循环 JIT_THRESHOLD=100 后命中 | 解释器路径的溢出 trap 在 JIT 路径变 panic |
| V-P0-5 | **`host_qsort`/`host_bsearch` 的 `nmemb * size` usize 乘法溢出** | `cide_vm/src/host/misc.rs:172,178,253`；VFS `fread/fwrite` 同类（`vfs.rs:227,300`） | `qsort(base, 2^32, 2^32, cmp)`：乘积 wrap 为 0 → 边界检查通过 → `(0..2^32).collect()` 分配 ~32GB → OOM abort；debug 构建乘法直接 panic |

### 3.4 Flutter 前端 — 5 条〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕

| # | 问题 | 位置 | 证据摘要 |
|---|---|---|---|
| U-P0-1 | **WatchTab 在 build 中创建 TextEditingController**：每次 rebuild 泄漏一个 ChangeNotifier，且打断用户输入 | `widgets/watch_tab.dart:24` | 统一模式运行期间每次状态变化 rebuild → 永不 dispose 的 controller 累积；TextField 挂新实例导致输入被清空 |
| U-P0-2 | **语法高亮层每帧每行 `new TextPainter + layout()` 且不 dispose**（文本整形是最昂贵操作之一；Flutter 3.13+ 需显式 dispose 否则泄漏 ui.Paragraph） | `editor/syntax_highlight_layer.dart:29-47`；`editor_panel_v2.dart:552-557`（每次 build 新建 layer 实例，缓存随之丢弃） | editor_painter 已有按行缓存却被完全绕过 |
| U-P0-3 | **编辑器静态 TextPainter 缓存无上限、跨文档污染、永不清理** | `editor/editor_painter.dart:37`（`static final Map`，进程级共享，key 仅行号） | 多文件 tab 互相踢缓存（thrashing）；长文档滚动后所有行永久驻留；进程内存只增不减 |
| U-P0-4 | **Gutter（行号/热力图列）不随编辑器滚动更新** | `widgets/editor_panel_v2.dart:619-625`（scrollOffset build 时一次性求值）；`editor/cide_editor.dart:186-188`（滚动只 setState 重建 CideEditor 自身子树，不通知父级 Row 中的 GutterView） | 滚动过程中行号列静止不动，靠 provider 状态变化"偶发"追赶刷新 |
| U-P0-5 | **`shouldRepaint` 只比较 `layers.length`，运行时高亮/诊断内容变化不触发重绘** | `editor/editor_painter.dart:105-115` | 单步执行行高亮、变量访问高亮停留在旧帧，仅靠滚动/输入副作用"偶发"刷新 |

### 3.5 工程体系（CI / 测试防线）— 4 条

| # | 问题 | 位置 | 证据摘要 |
|---|---|---|---|
| E-P0-1 | **C 版 Shadow Verification 不是 CI 门禁，任何回归恒绿** | `native/tests/shadow_verification/shadow_verify.py` `main()`（约 815-935 行）：无任何 `sys.exit(非零)` 路径；CI 调用于 `.github/workflows/ci.yml:77-79` | 对比 C++ 版 `scripts/shadow_verify_cpp.py:577` 有 `return 0 if not unexpected_gaps else 1`。防线 1 在 CI 中只是"出报告的观测工具" |
| E-P0-2 | **ci_three_tier_check.py 不检查 cargo 退出码，编译失败被误判 PASS** | `scripts/ci_three_tier_check.py:85-102,313`：只正则解析 `test result:` 行；找不到时返回全 0 → `failed==0` → PASS；`proc.returncode` 从未读取 | cargo 编译错误/依赖拉取失败 → 三层检查显示 PASS 并 return 0 |
| E-P0-3 | **「*_FAILURES.md 交叉验证」不阻塞 CI，与 AGENTS.md 防线 5 声明矛盾** | `scripts/ci_three_tier_check.py:332-336`（issues 只 `[WARN]` 打印）、`:345`（最终只看 `r.passed`） | 「KNOWN_FAILURE 现在通过 → 报错」与「失败但无记录 → 报错」两个方向都未实现进退出码 |
| E-P0-4 | **clang 缺失时防线 1 静默降级为「全量 cide_better」** | `shadow_verify.py:39`（`CLANG_PATH="clang"` 无预检）；`analyze_diff` 尾分支（clang 失败且 cide 成功 → `cide_better`）；ci.yml 无 clang 安装/版本步骤 | runner 镜像变更导致 clang 不在 PATH → 所有用例被吞异常归类 `cide_better` → 报告反而"更好看"且 exit 0（叠加 E-P0-1）。Golden 生成也从不校验 clang 版本 |

---

## 4. P1 发现（56 条 — 应尽快修复）

### 4.1 编译器前端（11 条）

1. **`#if`/`#elif`/`#undef` 完全未实现，`#if 0` 块参与编译**（实测）— `cide_lexer/src/preprocessor.rs:97-155` 指令 match 只覆盖 define/include/ifdef/ifndef/else/endif。`#if 0` 中合法代码仍被编译，还报误导性的 `E1011 没有匹配的 #ifdef`。
2. **零参数函数式宏 `#define FOO() 42` 完全不可用**（实测）— `preprocessor.rs:308-357` 与 `expand.rs:25` 用 `params.is_empty()` 判定对象宏，`FOO()` 展开为 `body ()` 报 E3045。
3. **浮点科学计数法 `1.5e3` 不支持且报误导性错误**（实测）— `cide_lexer/src/number.rs:60-87` 无指数部分；被切为 `1.5` + 标识符 `e3`，报「预期 ';'」。C语言子集规范 未记录此限制。
4. **八进制转义 `\012` 与一位十六进制转义 `\x4` 静默产生错误字节**（实测）— `cide_lexer/src/string.rs:35,36-52`；`"A\012B"` → sizeof=6（clang 4）；char 与 string 两处行为还不一致。
5. **初始化列表/实参列表不支持 C99 尾逗号**（实测）— `cide_parser/src/postfix.rs:262-265,286`；`int a[]={1,2,3,};` 报 E2003；enum 列表反而已支持，行为不一致。
6. **自定义 `#include` 后主文件 SourceLoc 行号整体偏移**（实测）— `preprocessor.rs:112-128` 字符 splice 无行号映射；3 行头文件导致后续报错偏移 +2。诊断、断点、教学定位全部错行。
7. **递归/自包含 include 无防护，可无限 splice 挂死/OOM** — `preprocessor.rs:110-129` 无深度计数与已包含集合。
8. **宏参数数量不匹配静默不展开** — `expand.rs:67-72` 原样保留 token，产生「未定义函数 MAX」类误导诊断。
9. **struct 紧凑布局与 clang 对齐不一致且未记录**（实测 `struct{char a;int b;char c;}` sizeof=6，clang 12）；`sizeof(char)` 上下文不一致（顶层 4 / struct 内 1）— `cide_ast/src/lib.rs:55-62`；布局规则本身未写入已知差异清单。
10. **超大数组维数 i32 乘法溢出无检查**（实测 `int a[100000][100000]` 无诊断挂死）— `cide_parser/src/type_.rs:479-483`、`cide_ast/src/types.rs:229-243,555-571,591`。
11. **非法八进制 `08` 拆成两个 token 报误导错误**（实测）— `number.rs:47-58`；报「预期 ';'」而非 clang 的「invalid digit '8' in octal constant」。

### 4.2 类型检查 / 代码生成（12 条）

1. **`&&`/`||` 结果非规范化 0/1**（实测 `5&&3` 输出 3、`0||7` 输出 7，clang 均为 1）— `cide_codegen/src/expr/binary.rs:50-76`。
2. **struct 按值拷贝/返回 `size/4` 截断**（`{char a;char b}` 拷 0 字节、`{int x;char c}` 丢 c）— `expr.rs:471`、`stmt/control.rs:140`；对比 `call.rs:53` 的 `(sz+3)/4` 三处不一致。
3. **`is_int` 不含 LongLong 造成系统性误拒**：`switch(ll)`、`p+1LL`、`a[ll]`、`a & b`（long long）全部报错 — `cide_typeck/src/convert.rs:77-79` 波及 5 处。
4. **局部 struct/嵌套数组初始化列表不足字段不清零**（C 要求零初始化）— `stmt/var_decl.rs:500-543`（designator 路径有 Memset，非 designator 没有，自身不对称）。
5. **void* 算术 `+`/`++` 步长为 0**，与 `+=`（步长 1）不一致 — `cide_ast/src/lib.rs:41` + `binary.rs:127,132` + `unary.rs:150-158` vs `assign.rs:41-53`。
6. **函数指针类型不兼容赋值仅警告放行**（clang 是 error），后续调用参数布局错位 — `convert.rs:179-185`。
7. **局部变量 shadow 函数名：typeck 与 codegen 解析顺序相反** — `expr.rs:57-60`（codegen 先查函数）vs `expr/var.rs:11`（typeck 变量优先）；`int f(){int f=1; return f;}` 返回函数索引。
8. **`gen_addr` 不支持 static 局部变量** — `expr.rs:379-403` 缺 `static_local_indices` 分支；`static struct S s1; struct S s2 = s1;` 报"未声明的变量"。
9. **嵌套 `new` 与构造 double 实参的槽位/宽度缺陷** — `new_delete.rs:149-178`（嵌套 new 覆盖 temp0）；`cpp/var_decl.rs:34-65`（构造调用缺 SplitD/SplitQ）；`new double(2.5)` StoreMem 截断。
10. **同参数数量的普通方法重载 mangling 冲突且无歧义防线** — `cpp_class_layout.rs:304-309`（仅按参数个数）；`void f(int)` 与 `void f(double)` 同名，先注册者胜，静默错误。
11. **循环外的 switch 中 `break` 错误析构函数体层对象** — `stmt/control.rs:191-197`（switch 不 push loop 深度）；`Foo a; switch(x){case 1: break;} a.use();` 中 a 被提前析构。
12. **浮点两处系统性偏离未记录**：(a) `CastI2D` 按 `as i32 as f64`，`(double)u`（unsigned 值 4294967295）得 -1.0 — `cide_vm/src/core/executor/float.rs:124-127`；(b) 所有浮点比较用 EPS=1e-6 绝对容差，`0.1+0.2==0.3` 判真（clang 判假）— `state.rs:19-23`、`float.rs:141-170`。

### 4.3 VM 与运行时（14 条）

1. **JIT 与解释器算术语义分歧**：`Add/Sub/Mul` 解释器溢出 trap 教学诊断，JIT 模板 `wrapping_*` 静默——同一程序同一错误第 101 次执行起行为不同 — `jit_templates.rs:243-262,586-591`。
2. **JIT bulk 执行跳过断点与热力图**：trace 命中后一次最多 1000 轮循环，断点完全不触发、当前行冻结、热力图漏计 — `jit_trace.rs:85-88` + `executor/mod.rs:15-43`。对以"单步+断点"为核心卖点的教学 IDE 是功能性缺陷。
3. **`set_errno` 写错地址，errno 机制完全失效** — `host/utils.rs:416-423` 用符号相对偏移作绝对地址（正确用法见 `core/memory.rs:230` 的 `GLOBAL_START +`）。
4. **VFS 不纳入 VMSnapshot，时间旅行回退后文件状态来自"未来"** — `snapshot.rs:55-80` 无 vfs 字段；`fopen("w")→fwrite→回退→重放` 看到未来数据。VFS 自身有 `snapshot_files/restore_files`（`vfs.rs:656-674`）但未接入。
5. **`fopen(path,"a")` Append 建文件分支是死代码** — `vfs.rs:171-191`（Append 嵌在 Write 块内部恒 false）；`fopen("new.txt","a")` 后 fwrite 静默丢弃返回 0，C 标准应建文件。
6. **栈上缓冲区溢出零检测**：`strcpy/strcat/scanf("%s")` 的容量检查只覆盖堆 region，VM 栈区（`char buf[4]`）静默覆写相邻局部变量，无 E3070 无 trap — `host/string.rs:92-108,189-205`、`host/io.rs:125-143`。这是教学 IDE 最需要捕获的经典错误。
7. **`strncpy` 负数 `n` 清零整个内存高端**（含整个栈区）— `host/string.rs:219,227,232-234`（`n=-1` → `n as usize` → 写 0 到 1MB）。
8. **`host_memcpy/memmove` 绕过 NULL 陷阱区检查**（与 `Memcpy` opcode 行为不一致）— `host/string.rs:243-247,266-270`。
9. **时间旅行每步全量快照 1MB memcpy + output/trace 全量 clone，O(n²)**：10 万步批量 ~100GB 级 memcpy 流量 — `unified/engine.rs:186-187` + `core/snapshot.rs:84-114` + `snapshot.rs:105-118`。"执行前快照"仅用于 Trap 回退，可改用已有 CheckpointManager。
10. **调用栈无独立深度上限**：`int f(){return f();}` 零局部变量 → 栈堆碰撞检测永不触发 → call_stack 无限增长（Android OOM 风险）；且每次 Call/Ret `rebuild_local_sym_map()` 遍历全部 symbols — `core/executor/control.rs:31-108,106,211,231`。
11. **UAF 检测 O(n) 线性扫描挂在每次内存读写上；regions/freed_logs 无限增长**：`malloc/free` 循环程序二次方退化 — `core/memory.rs:188-192` + `host/memory.rs:41-50,259-268`。
12. **`host_free` 对非分配起始地址静默忽略**：`free(p+4)`/`free(栈地址)` 静默"成功"，教学场景双重误导（以为释放成功 + 泄漏报告不出现）— `host/memory.rs:75-94`。
13. **scanf 每次"按行消费"与 C 流式语义不一致**：`"1 2\n3 4"` 输入下第二次 `scanf("%d")` 读到 3（clang 应读 2）；`%c` 读不到换行 — `host/io.rs:44-46`（getchar 已有字符流游标机制 `io.rs:163-198`，scanf 未复用）。
14. **JIT bulk 步数统计双倍计入**（`jit_stats.steps_accelerated` 虚高一倍）— `jit_templates.rs:735,738`。

### 4.4 Flutter 前端（13 条）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕

1. **巨型 IdeState 单状态 + 全工程 0 处 `select`**：每敲一个字符 → 全树重建（IdeScreen/Toolbar/BottomPanel 全部 Tab/EditorPanelV2/悬浮球 Overlay）— `models/ide_state.dart:60-121`、`providers/notifiers/file_notifier.dart:5-14` 等 5 个大消费点；TODO#D09 自知。
2. **统一模式收集期通知风暴**：每批（100 步）2 次 state 写入 + 4 次 FFI 内存拉取（收集期对 UI 无意义的 memoryRegions 也每批拉）— `providers/unified_notifier.dart:115-124,136-153`。
3. **stepNext/seekTo/run 无重入保护，连续点击交错竞态**（frameCache 读-改-写窗口明确存在）— `unified_notifier.dart:566-602,606-664`、`run_notifier.dart:6-31`。
4. **`copyWith(error: null)` 语义 bug**：四处传 `error: null` 实际不清除错误（`?? this.error` 合并为无操作），面板交换后旧 error 残留 — `panel_notifier.dart:72,108,153,176` + `models/ide_state.dart:179`。
5. **ExecutionControlPanel 每次构建全量扫描 frameCache（上限 5000 帧 × 每帧 5 组字符串 contains）** — `execution_control_panel.dart:297-324`。
6. **VarHistoryTab 每次构建 O(帧数×变量数) 全量重算** — `var_history_tab.dart:14-25`。
7. **链表/树可视化每节点 2-3 次串行 FFI + `_loadNodes` 重入竞态**（旧任务旧数据覆盖新任务）+ 绕过 `RustApiService` 抽象直连 `rust.*` — `linked_list_visualizer.dart:131-150`、`tree_visualizer.dart:152-154,61-67`。
8. **悬浮球呼吸动画与数组脉冲动画永久 repeat + paint 内每帧新建 ~15 个 Paint/Gradient/MaskFilter**（移动端耗电）— `floating_orb_widget.dart:87-90,574-773`、`array_visualizer.dart:40-43`。
9. **EditorPanelV2 长按 Timer 未在 dispose 取消** → 组件销毁后弹出上下文菜单用 defunct context 崩溃 — `editor_panel_v2.dart:410-424`（dispose 无 `_cancelLongPress()`）。
10. **AutocompleteController 防抖 Timer 无 dispose/cancel**，销毁后 notifyListeners 抛断言 — `autocomplete_controller.dart:91-94,102-147`。
11. **热路径 debugPrint 洪水**（每次按键打印整个文档文本、拖动每帧打印；release 下照常执行字符串拼接）— `cide_editor.dart:211-277,473-475`、`floating_orb_widget.dart:147-151,318-349`。
12. **`_onScrollChanged` 用 setState 整树重建 CideEditor**（应仅重绘 CustomPaint）— `cide_editor.dart:186-188`。
13. **`_showAddFileDialog` 的 TextEditingController 泄漏** — `screens/ide_screen.dart:126-163`。

### 4.5 工程体系（6 条）

1. **`run_auto_steps_stream` 无防重入**：双线程并发 `session.vm.take()` → 线程 B 拿到空默认 VM 并写回覆盖线程 A 进度；且整批执行期间持锁阻塞所有前端 API — `flutter_bridge.rs:729-746,608,627`。
2. **Session/UnifiedEngine Map 无上限无回收**；`destroy_session(0)` 可移除共享默认 session；多 Session API 前端 0 调用（已暴露未使用）— `flutter_bridge.rs:44-59,74-88`。
3. **E2E golden 缺失时测试静默通过**（读不到 .out 返回 None 直接 Ok），防线 2 可退化为 smoke test — `cide_e2e.rs:151-161,214-218`。
4. **测试统计四处口径互相矛盾**：E2E_FAILURES.md 统计表 78+4 vs 正文 3 条 vs KNOWN 常量 3 个；LEETCODE_FAILURES.md「68 题」vs 实际 138 题；AGENTS.md 内部 314/69 vs 316/81 自相矛盾 — 多文件多处。
5. ~~**Shadow「完全匹配」建立在对 Cide stdout 的正则清洗之上，且清洗逻辑三处重复**（Python 两份 + Rust 一份）；教学程序自己打印"程序运行完成"文本会被误清洗 — `shadow_verify.py:220-224`、`shadow_verify_cpp.py:455-460`、`cide_e2e.rs:100-113`。~~ **✅ 已修复（E-P1-5，2026-09-11）**：根因是 `RuntimeState::output_lines` 一条字节流同时承担"程序 stdout / 程序 stderr / 引擎附注"三种语义（且附注可在流中间插入，如堆耗尽提示）。现改为带 `OutputKind` 的分段输出（`output_chunks` + `stdout()`/`stderr()`/`notes()`/`display()` 投影），新增 capi 出口 `cide_get_program_output*` / `cide_get_engine_notes*` / `cide_get_program_output_delta`（ABI 1.0.0 → 1.1.0，加函数=minor），serve 的 `output.delta` 增加 `stream` 参数。**十余处清洗规则全部删除**——实际数量远超本条记录的"三处"：`bytecode_libc_consistency.rs`、`test_utils.rs`、`bytecode_gen_cpp_unit_test.rs`、`end_to_end_extra_test.rs`、`qsort_test.rs`、`test_more.py`、`test_massive.py` 等。回归用例 `native/tests/cases/baseline/engine_note_lookalike.c`（程序打印与引擎附注逐字相同的文本 + 走一次 stderr + 无尾换行收尾）已纳入 Shadow 与 E2E 防线。
6. **`apply_fix` 按字节列切片字符串，含中文的行 panic**（原经 FRB 返回错误，前端"一键修复"崩溃；**FRB 边界已随前端切割移除**）— ~~`native/src/api/cide.rs:241-249,292-295`~~（**文件已随切割移除**；修复本体现位于语言中立的 `native/src/diagnostics/auto_fix.rs`，三出口可复用，`crash_regression_tests` 的 3 个相关用例已改走新入口）。（error_catalog 的字节坐标与字符语义混用）

---

## 5. P2 发现摘要（56 条，按主题归纳）

- **前端健壮性/正确性**（14 条）：十进制超 u64 `unwrap_or(0)` 静默归零；`consume` 失败返回恢复点 token；回溯点快照不统一（sizeof 路径 typedef 注册副作用泄漏）；ops.rs 479 行复制粘贴模板代码；make_token 列号用字节长度（中文行偏移 2 倍）；typedef 无块作用域（`(T)-1` 歧义，实测 `int T=5;(T)-1` 输出 -1，clang 4）；`short`/单 `long` 静默丢弃；`null`/`bool`/`NULL` 关键字化；`TemplateArg::Expr` 无相等性恒 false；`set_const` 漏 3 分支；`#include <` 未闭合吞整个文件；宏体不支持 `\` 续行且宏体错误行号错乱；Try/Catch 死 AST 节点；Call/CallPtr 双轨与 `__` mangling 嵌套碰撞风险；`parse_template_arg_expr` 子流无 EOF 哨兵。
- **typeck/codegen 质量**（10 条）：26 处 `unreachable!()` panic 路径 + `evaluate_constexpr` i32 溢出 debug panic；`compute_type_size` 每次克隆全量类型表（O(n²)）；常量池线性查重；switch `stmt.clone()` 深拷贝/体内声明静默丢弃/case 重复无检查；局部与匿名 struct 定义不支持（未记录）；goto 跳出作用域不析构；static 非字面量初始化每次进函数重跑；typeck/codegen class 大小含不含 vptr 不一致（offsetof 少 4）；派生类新增虚函数而基类无 vtable 时 vptr 覆盖基类首字段；20+ 杂项语义差异（`char s[3]="abc"` 报错、三目不做 usual conversion、`(char)300` 不截断、Float→LongLong 无指令、unsigned LongLong 用有符号指令执行、全局 designated initializer 实际不支持但 AGENTS 声称已支持等）。
- **VM 运行时**（14 条）：浮点除零 trap 偏离 IEEE（应记录）；`strchr/strrchr/memchr` 符号扩展与 `strcmp` 不一致；`host_strlen` 无 NULL 检查与 `Strlen` opcode 不一致；fseek 二进制负 offset 变巨大 usize；sprintf 缺参数栈检查且无容量检查；restore 不重建 `local_sym_map`（seek 后变量名错误）；`time()/clock()` 真实墙钟破坏重放确定性（rand_seed 已恢复而 time 没有）；`rand()` 只取低 15 位与 glibc 序列完全不同（应记录）；`call_user_function` 用 `assert!` 校验；地址计算 u32 加法依赖编译器产物善意（bytecode bundle 导入打开此面）；UAF freed_log 部分重叠整条删除漏报；`host_strerror` 不进 region 追踪；检查点全量 clone output（同 O(n²) 根因）；每步 `lines().nth()` 源码扫描。
- **Flutter**（12 条）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕：`focusNode` getter 每次兜底 new（泄漏）；行高 21.0/字号 14/字体名硬编码散落 15+ 文件；ConceptGraphView `Set != Set` 恒真比较 + await 后无 mounted；learning_path_panel `async void` + 跨 async gap context + 空列表 `firstWhere` 崩溃；语义补全 stale response 竞态；`_outputController` 在 provider rebuild 时 dispose-复用地雷；`_loadProgress` 微任务竞态覆盖用户进度；空安全杂项（`confirm()!`、`inputController!`、`ApiVariableSnapshot` 全 dynamic、`applyFix` 无 orElse）；高频路径重复分配（每行每词 new RegExp、每帧全文比较、TextPainter 不 dispose）；`compute()` spawn isolate 方案待基准；光标无闪烁；可访问性缺失（自绘编辑器无 Semantics、颜色语义无标签、触控目标 <44px）。
- **工程**（6 条）：C API 输出缓冲契约陷阱（length 不含 NUL 而 max_len 含，`buf[length]` 用法截断末字符）；CI 4 次冗余 Rust 编译 + 多次工具链安装、覆盖率无门禁；Python 脚本硬编码个人机器路径（`D:\Program Files...`）+ `find_flutter()` 逐字重复两份（**前端脚本已随切割迁出，该子项关闭**）；仓库卫生（`tmp/` 57 个临时测试文件被 git 跟踪、`lines.txt` 被跟踪、根目录 `tmp_threaded*.exe` 4 个二进制残留、`out.txt/numbers.txt/test.txt` 由 shadow 脚本写入 CWD 从不清理、`build/` 旧 CMake 产物与 Phase 5「已清理」声明不符、`reports/` 混杂调试残留）；测试盲区（`algorithm_detector` 8 文件 0 单测、`error_catalog.rs` 455 行 0 直接测试、`ERROR_INFO_MAP` 重复错误码静默覆盖无校验）；脚本死代码与跨平台半成品（`EXPECTED_DIVERGENCES` 未引用、DLL_PATH 硬编码 .dll 但有非 Windows 分支、`infer_intent_from_source` 每次全量 lex+parse 无缓存）。

---

## 6. 修复优先级路线图

**第一批（崩溃止血，1~2 天）**
1. V-P0-3 统一模式三入口加 catch_unwind（照抄 `execute_run` 模式）— 防 FFI panic UB
2. V-P0-1/2/4 + JIT 模板：Mod/DivQ/ModQ/NegQ 补 `MIN / -1` 防护
3. F-P0-1：lexer 注释分支循环化 + parser 深度计数器（统一 DepthGuard）
4. T-P0-8：自含 struct 环检测 + `compute_type_size` visited
5. V-P0-5：qsort/bsearch/VFS `checked_mul` + 上限
6. E-P1-6（apply_fix 中文 panic，前端一键修复崩溃路径）

**第二批（CI 门禁，1 天）**
7. E-P0-1：shadow_verify.py 补 exit code（照抄 C++ 版 expected/unexpected 逻辑）
8. E-P0-2：ci_three_tier_check.py 检查 returncode 与 test result 行缺失
9. E-P0-3：consistency issues 计入退出码
10. E-P0-4：clang 预检 + 版本记录，缺失 fail fast

**第三批（codegen soundness 8 条，2~3 天，全部有现成复现用例）**
11. T-P0-1/2（全局初始化隐式转换 + long long 位模式）→ T-P0-3/4/5（类型化读写宽度）→ T-P0-6（temp_slot 审计）→ T-P0-7（static mangle）→ F-P0-2/3（unsigned long long / enum 常量折叠）
12. 同批顺带 T-P1-1（`&&`/`||` 规范化）与 T-P1-2（struct 拷贝 `(sz+3)/4`），并把这 10+ 个实测复现用例固化为回归集

**第四批（Flutter 编辑器与状态）〔前端侧，2026-09-11 已随前端切割迁出，条目关闭不再推进〕**
13. ~~U-P0-1~P0-5 编辑器渲染管线重构（文档级缓存 + shouldRepaint 内容签名 + scrollOffset ValueListenable）~~ —— 条目关闭（前端资产已迁出）
14. ~~U-P1-1 IdeState 拆分/select 接入；U-P1-3 防重入守卫；U-P1-4 copyWith error 语义；U-P1-9/10 Timer 生命周期~~ —— 条目关闭（前端资产已迁出）
15. **E-P1-1 run_auto_steps_stream 防重入令牌；E-P1-2 Session 上限** —— **后端侧，仍按计划追踪**（落点 `native/src/flutter_bridge.rs`）

**提前插入（与教学目标直接冲突的漏报，建议第三批并行）**
16. V-P1-6 栈缓冲区溢出检测（E3070 教学 Core 价值）+ V-P1-12 无效 free 诊断 + V-P1-13 scanf 字符流化

---

## 7. 框架与架构迭代建议（按收益排序，跨层去重归纳）

### 7.1 编译器层
1. **统一递归/规模预算**：`cide_shared::DepthGuard` 全链路（Lexer/Parser/TypeChecker/Codegen）统一深度上限，Fuzz 防线新增「深嵌套/长注释/多星号」corpus（根治 F-P0-1 家族）。
2. **预处理器独立成 pass + 行号映射**：当前预处理寄生在 Lexer 字符流内（splice+跳行）是 `#if` 缺失、行号偏移、递归 include、宏体续行 4 类问题的共同根因。拆出独立 Preprocessor 输出 token 流 + `LineMap`，`SourceLoc.file_id` 字段已存在但全程写 0，正好启用；同时为多文件编译铺路。
3. **Type 系统引入 QualType 包装**：`is_const/is_unsigned/set_const` 从 15 个变体外置为 `QualType { base, quals }`（clang 同构），删除 ~200 行样板与 75 行手写 PartialEq，从结构上杜绝 `set_const` 漏分支家族。
4. **字面量解析单点收口**：lexer 一次性产出 `(数值, 类型标签, 后缀)` 结构化 payload，消除 parser 侧 `parse::<u32>() as i32`、`trim_end_matches('f')` 等脆弱点（F-P0-2 根因）。
5. **错误恢复契约化**：`consume` 返回 `Option`；回溯点封装 `Checkpoint { pos, errors_len, typedef_names }` 统一 save/restore，写进 AGENTS.md 编码约定。

### 7.2 代码生成层
6. **统一内存读写宽度决策函数**：`load_opcode(ty)/store_opcode(ty)` 收口 30+ 处手写 match（Char 分支遗漏家族的根因），新类型只改一处。
7. **临时槽位作用域化分配器**：temp_slot0~3 全局固定 + 隐式"立即消费"约定已产生 3 起同类 bug（历史 side_effect_index、本次嵌套复合赋值、嵌套 new）。改线性 bump 分配器或至少 debug 断言不重复占用，一次性根除。
   > **2026-09-11 补充（第四起，已局部修复）**：SharpTutor Issue B2 是同一家族的新成员，但根因方向相反——不是槽位重复占用，而是**槽位少算**：`gen_lambda` 在栈上推的是闭包**对象地址**、lambda 变量槽里存的也是地址（4 字节），而槽位大小按闭包类字段总大小计算，无捕获闭包 size=0 → 帧内根本没有槽位，`StoreLocal` 直接冲出 1MB 线性内存（实测 `locals_base=1048572`、`operand=4`、`addr=1048576`）。触发条件为"先立即调用 lambda、后声明 lambda 变量"。已引入 `is_lambda_closure_type` 单一判定（变量槽位与闭包对象保底 4 字节、实参一律 1 word）局部修复；**分配器整体重构（本条原议题）仍未启动**，`next_local_offset` 只增不减、闭包对象每次求值重复分配的问题仍在。
8. **typeck 与 codegen 收敛到单一语义模型**：符号解析优先级、类型大小（vptr）、整数类别（is_int）、指针步长、unsigned 转换全部下沉到共享 crate（如 `cide_runtime::semantics`），两层只调不改。
9. **建立 codegen 语义回归层**：本次 8 个 P0 全部漏过五层防线（shadow 用例未覆盖边角组合）。把复现用例固化为 `cide_cli run` + stdout 断言回归集；补"生成层栈平衡/槽位冲突"debug 校验器。

### 7.3 VM 层
10. **统一安全算术执行层**：解释器与 JIT 模板收敛到单一 `op_*` 语义函数，JIT 的加速点放在跳过 dispatch 与簿记而非复刻语义（V-P0-1/2/4、V-P1-1 共同根因）。
11. **内存元数据升级为有序区间结构**：regions/freed_logs 改 BTreeMap/排序 Vec + 二分（当前每次读写 O(n) 扫描是解释器最大性能天花板）；在此之上做统一写路径校验器（NULL 区/1MB 边界/UAF/栈帧越界一次完成），host 函数只允许经批量接口写内存。
12. **时间旅行快照分层 copy-on-write**：trap 回退改用"最近检查点 + dirty 页"重建；output/trace 改 `(长度, 回退追加日志)` 截断式恢复；目标把每步快照从 O(1MB) 降到 O(改动量)。
13. **快照边界完整化**：VFS、local_sym_map、伪时间纳入快照/恢复闭环，`time()` 改 step 派生确定性时钟；文档化不可恢复项清单。
14. **JIT 与调试器协同**：trace 命中后保留 StepEvent 或周期性断点检查；含断点的循环排除出 trace 编译。

### 7.4 Flutter 层〔前端侧，2026-09-11 已随前端切割迁出；以下建议冻结存档，不再作为本仓待办〕
15. **拆分 IdeState + 全面接入 select**（收益最大的单点改造）：按域拆 sourceProvider/panelLayoutProvider/learningProgressProvider/runStatusProvider；过渡期先在 5 个大消费点切片。
16. **编辑器渲染管线版本化**：以 CideDocument 为缓存宿主（版本号递增），TextPainter/高亮缓存挂文档实例而非 static；layers 纳入内容签名；scrollOffset 用 ValueNotifier 驱动局部重绘。
17. **Riverpod 2.x → 3.x 评估**：代码已用新式 Notifier API 迁移成本低；AsyncNotifier 可统一 compile/run 手工状态管理（isCompiling/error 手工管理与 7 相枚举双轨制可收敛）。
18. **可视化数据分发层统一**：派生切片 provider + 记忆化（事件标记/变量历史 O(n) 重算）；Rust 端补批量读内存接口（每节点串行 FFI 的根因）；RustApiService 收口为唯一 Rust 入口。
19. **常量与日志治理**：EditorTheme/IdeColors 常量类（21.0/14/Consolas/40+ 处 isDark 三元）；统一 logger + kDebugMode 门控。

### 7.5 工程层
20. **两条 Shadow 防线"带牙齿"**：exit code + clang 预检 + known_issue 显式登记；stdout 清洗收敛为 VM 原始输出通道（教学程序打印"程序运行完成"会被误清洗）。
21. **对账权威从 Markdown 迁移到代码常量**：`cide_e2e.rs` 的 `KNOWN_*_FAILURES` 双向监控是唯一真正闭环的机制，FAILURES.md 机器可读部分由脚本从常量生成；统计数字单一事实源（CI 导出 JSON，文档引用）——本次已发现 4 处统计漂移，直接侵蚀「诚实记录」原则可信度。
22. **仓库卫生一次清零 + 防复发**：`git rm --cached tmp/ lines.txt`；shadow 脚本改写 tempfile 目录；「根目录无未跟踪垃圾」挂进 `engineering_health.py` 检查。
23. **覆盖率门禁 + 盲区补测**：llvm-cov 加 `--fail-under-lines` 与模块报表；`algorithm_detector`/`error_catalog` 补表驱动单测；CI 构建产物经 artifact 传递消除 4 次冗余编译。

---

## 8. 「诚实记录」补录清单（按项目原则应写入 C语言子集规范 / AGENTS.md 已知差异）

以下行为差异经实测确认但未在现有「已知限制/差异」文档中记录：

1. `#if`/`#elif`/`#undef` 不支持（且 `#if 0` 块参与编译产生误导错误）
2. 浮点科学计数法字面量（`1.5e3`）不支持
3. 八进制/一位十六进制转义 `\012`/`\x4` 产生错误字节
4. 初始化/实参列表不支持尾逗号（C99）
5. enum 初始化器仅支持纯字面量（`-1`、`1+2` 静默取错值）
6. struct 紧凑布局无对齐填充（sizeof 与 clang 系统性不同）；`sizeof(char)` 顶层 4 / struct 内 1 上下文不一致
7. `&&`/`||` 结果非 0/1 规范化
8. 浮点比较使用 1e-6 绝对容差（`0.1+0.2==0.3` 判真）；`(double)unsigned` 符号错误
9. `void*` 的 `+`/`++` 步长为 0（仅 `+=`/`-=` 是 1）
10. 局部与匿名 struct 定义不支持
11. 全局/静态 designated initializer 不支持（AGENTS 声称 Phase 30 已支持，实际仅局部）
12. `rand()` 只取低 15 位，与 glibc/Clang 序列完全不同
13. `sizeof(short)` 为 4（clang 2）；`short` 修饰符静默当 int
14. `null`/`bool`/`NULL` 被关键字化（`int null = 5;` 报错，clang C 接受）
15. scanf 按行消费语义（多次 scanf 一行多值场景与 clang 不一致）
16. `char s[3] = "abc"`（精确长度无 `\0`）报错
17. typedef 无块作用域（局部变量遮蔽 typedef 后 `(T)-1` 仍解析为 cast）

---

## 9. 审阅方法与局限

- 5 路并行审阅均逐文件精读（前端 26 文件约 7800 行、typeck/codegen 53 文件、VM/runtime/unified/engine 全部、Flutter 107 文件中自研部分、工程体系全部脚本与 CI），P0 级发现尽可能用 release 版 `cide_cli` 实测复现。其中前端/Flutter 部分已于 2026-09-11 随前端切割迁出，相关条目关闭（原文保留）。
- 局限：本次未跑完整 E2E/Shadow 防线（Clang 环境依赖），未审计 `frb_generated.rs`/`frb_generated.dart` 生成代码内部（**两文件已随 2026-09-11 前端切割移除**）；Flutter 发现基于静态审阅未做 profile 实测帧率〔前端侧，已随前端切割迁出，条目关闭不再推进〕；P2 中个别「实测」标注来自子代理用 cide_cli 验证，主审阅未逐一复跑。
- 本报告只读审阅，未修改任何源码；报告文件本身为新增（不覆盖 2026-06-13 旧报告），未做 git 提交。
