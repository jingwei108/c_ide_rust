# R3 语义单源审计清单（2026-09-12）

> **已归档（2026-09-13）**：R3 批次验收线即本清单归档：A 批收口 3 项 / B 历史复核 5 项确认 / C 有意保留 3 项均已归属后续批次（C1→CS0、C2→CS5、C3→R4 已完成）。
> 本文件仅作历史追溯保留，内容不再维护；当前有效文档见 `docs/current/`。


> 重构计划批次 R3：以 E-P1-5 的模式扫描全库"同一概念多个真相来源"的残留点。
> 每项记录：病灶 / 收口方式 / 状态。验收线 = 审计清单归档（本文档）+ 标注矛盾
> 类缺陷结构性消除。

## A. 本批收口（已完成）

| # | 病灶 | 双真相表现 | 收口方式 |
|---|------|-----------|---------|
| A1 | 教学语义标注双来源 | `unified/engine.rs::quick_semantic_label`（"循环边界"/"交换"）与 `unified/collector.rs::infer_semantic_label`（"循环"/"交换 arr[i]↔arr[i+1]"）——同一行源码两处标注词汇不一致（P0-3 曾记录过同类矛盾事故） | 删除 quick 版；`infer_semantic_label` 升级为**全库唯一分类器**（`local_vars: Option` 双形态：全量/降级），检查点判定与 StepPayload 标注同函数同词汇 |
| A2 | 堆耗尽消息硬编码数值 | `report_heap_exhausted` 文本硬编码 "1MB / 256KB"（与 `MEM_SIZE` / `DEFAULT_QUARANTINE_BUDGET` 双写，改常量会漏改文案） | 消息改为 `format!` 自常量格式化；`DEFAULT_QUARANTINE_BUDGET` 补入 `cide_runtime` 导出 |
| A3 | `session.reset` 语义单点 | 配置保留式重置逻辑仅存在于 cide_cli（`serve_reset_session`），capi/serve 无法复用 | 迁至 `session_api::reset_session_preserving_config`（出口薄包装纪律） |

## B. 历史批次已收口（本批复核确认）

| # | 病灶 | 收口批次 |
|---|------|---------|
| B1 | 输出通道清洗（正则删"程序运行完成/泄漏报告"文本） | E-P1-5（note 通道分离） |
| B2 | 内存布局常量（`MEM_SIZE/16` vs `HEAP_START` vs 新增上限） | R1（`GLOBAL_REGION_LIMIT` 单源 + 双保险丝） |
| B3 | `get_heap_stats` 内联复算 vs `build_heap_stats` | R1（flutter_bridge 内联改走单源；R2 该文件整体删除） |
| B4 | 宏展开 H01 `do{}while(0)` 包装 | E2（新展开器后处理阶段唯一实现） |
| B5 | Mutex poison 恢复（`lock_or_reset`/`POISON_COUNT`） | R2（flutter_bridge 整删，问题域结构性消除，MAINTENANCE_PLAN D12 销项） |

## C. 有意保留（记录为差异/延迟项，非遗漏）

| # | 项 | 归属 |
|---|-----|------|
| C1 | `"main.c"` 兜底字符串 7 处（扩展名即真相，无第二来源，但属硬编码散布） | CS0（`SourceLang` enum 随语言分派单源化一并处理） |
| C2 | `semantic_label` 从自由文本启发升级为**受控词汇表枚举** | CS5（R3 收口项落地——本批 A1 是其前置：先单函数，后词汇表化） |
| C3 | 文档数字 vs 代码常量自动对账 | R4（G10+G12） |

## D. 扫描方法（可复现）

- `grep "1MB\|256KB\|0x5000\|0x1000"` → 引擎常量进文本（A2）
- `grep "drain(..).collect\|if let .* else { return"` → 同值双写/可 ? 化（R1/clippy 批次）
- 宏表对比：`runtime_libc/include/*.h` 的 `#define` vs `lexer/macros.rs` 内置表（无重叠：limits.h 存根仅为注释，float.h/stdarg.h 走存根唯一路径）
- 语义标注：`grep infer_semantic_label\|quick_semantic_label`（A1）
